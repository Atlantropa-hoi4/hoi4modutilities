import { technologyPreviewDef } from '../../previewdef/technology';
import type { ExtensionFeature } from '../types';

export const technologyFeature: ExtensionFeature = {
    id: 'technology',
    previewDescriptors: [technologyPreviewDef],
};
